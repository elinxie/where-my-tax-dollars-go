# Privacy

The public calculator keeps answers in browser memory and does not save them. It does not request a street address or employer name. A future location resolver should prefer ZIP code plus county confirmation, resolve locally when practical, and discard raw location input after jurisdiction matching.

Any future analytics, session replay, account, or saved-profile feature requires a separate review and an update to the user-facing privacy statement before launch.
